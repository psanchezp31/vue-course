const state = {
  // la funcion de flecha vuelve reactivo el objeto store
  count: 1, //porque si no quedaría un objeto sencillo
  lastMutation: "none",
  isLoading: false,
  lastRandomInt: 0,
};

export default state
